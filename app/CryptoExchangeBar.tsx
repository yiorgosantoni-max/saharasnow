"use client";
import {useEffect} from "react";
const exchanges=["BingX","Binance","Crypto.com","OKX","Bybit","Kraken","KuCoin","Coinbase"];
export default function CryptoExchangeBar(){useEffect(()=>{if(location.pathname!=="/")return;const install=()=>{if(document.getElementById("sahara-crypto-exchanges"))return;const popular=[...document.querySelectorAll<HTMLElement>("main *")].find(e=>/^\s*popular\s*:/i.test((e.innerText||"").trim()));if(!popular)return;const bar=document.createElement("section");bar.id="sahara-crypto-exchanges";bar.setAttribute("aria-label","Popular crypto exchanges");bar.innerHTML=`<div class="sce-copy"><span class="sce-icon">₮</span><div><strong>Pay with USDT</strong><small>Use your preferred crypto exchange or wallet to send USDT on Tron (TRC20).</small></div></div><div class="sce-brands">${exchanges.map(name=>`<span class="sce-brand">${name}</span>`).join("")}</div><p class="sce-note">Popular exchange and wallet options — brand names are shown for convenience and do not imply an official partnership.</p>`;popular.insertAdjacentElement("beforebegin",bar)};const timer=window.setTimeout(install,180);return()=>clearTimeout(timer)},[]);return null}
