"use client";
import {useEffect} from "react";
const exchanges=["Binance","Crypto.com","OKX","Bybit","Kraken","KuCoin","Coinbase"];
export default function CryptoExchangeBar(){useEffect(()=>{if(location.pathname!=="/")return;const install=()=>{if(document.getElementById("sahara-crypto-exchanges"))return;const popular=[...document.querySelectorAll<HTMLElement>("main *")].find(e=>/^\s*popular\s*:/i.test((e.innerText||"").trim()));if(!popular)return;const bar=document.createElement("section");bar.id="sahara-crypto-exchanges";bar.setAttribute("aria-label","USDT payment");bar.innerHTML=`<div class="sce-copy"><span class="sce-icon">₮</span><div><strong>Pay with USDT</strong><small>Send USDT on the Tron (TRC20) network. Scan the QR code or copy the deposit address during checkout. Zero deposit and checkout fees.</small></div></div><div class="sce-brands">${exchanges.map(name=>`<span class="sce-brand">${name}</span>`).join("")}</div><p class="sce-note">Use any compatible wallet or exchange. Brand names are shown only as examples.</p>`;popular.insertAdjacentElement("beforebegin",bar)};const timer=window.setTimeout(install,180);return()=>clearTimeout(timer)},[]);return null}
