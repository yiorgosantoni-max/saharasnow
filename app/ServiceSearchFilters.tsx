"use client";
import {useEffect} from "react";
export default function ServiceSearchFilters(){useEffect(()=>{if(location.pathname!=="/")return;let stopped=false;const install=()=>{if(stopped||document.getElementById("saharasnow-service-filters"))return;const popular=[...document.querySelectorAll<HTMLElement>("main *")].find(e=>/^\s*popular\s*:/i.test((e.innerText||"").trim()));if(!popular)return;const bar=document.createElement("div");bar.id="saharasnow-service-filters";bar.className="search saharasnow-exact-search";bar.innerHTML='<input id="ssf-search" type="search" placeholder="Search services..."><button id="ssf-search-button" type="button">🔎 Search</button>';popular.insertAdjacentElement("beforebegin",bar);const cards=[...document.querySelectorAll<HTMLElement>("main a[href*='/service/'],main article,main [data-service-card]")].filter(e=>e.querySelector("img"));const input=bar.querySelector<HTMLInputElement>("#ssf-search")!;const apply=()=>{const q=input.value.trim().toLowerCase();cards.forEach(card=>card.style.display=!q||(card.innerText||"").toLowerCase().includes(q)?"":"none")};bar.querySelector("#ssf-search-button")?.addEventListener("click",apply);input.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();apply()}});};install();const observer=new MutationObserver(install);observer.observe(document.body,{childList:true,subtree:true});return()=>{stopped=true;observer.disconnect()}},[]);return null}
