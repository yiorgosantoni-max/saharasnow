"use client";
import {useEffect} from "react";
export default function ServicePreviewPersistence(){useEffect(()=>{const key=(article:HTMLElement)=>`sahara-preview:${article.id.replace(/^service-/i,"")}`;const restore=()=>{document.querySelectorAll<HTMLElement>("article[id^='service-']").forEach(article=>{const img=article.querySelector<HTMLImageElement>(".visual>img");if(!img)return;const saved=localStorage.getItem(key(article));if(saved&&img.src!==saved)img.src=saved;else if(!saved&&img.currentSrc)localStorage.setItem(key(article),img.currentSrc);});};const remember=(event:Event)=>{const img=event.target instanceof HTMLImageElement?event.target:null;if(!img)return;const article=img.closest<HTMLElement>("article[id^='service-']");if(!article||!img.currentSrc)return;localStorage.setItem(key(article),img.currentSrc);};restore();document.addEventListener("load",remember,true);window.addEventListener("pageshow",restore);window.addEventListener("popstate",restore);return()=>{document.removeEventListener("load",remember,true);window.removeEventListener("pageshow",restore);window.removeEventListener("popstate",restore);};},[]);return null}
