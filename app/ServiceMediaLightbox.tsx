"use client";
import {useEffect,useState} from "react";
export default function ServiceMediaLightbox(){const [src,setSrc]=useState<string|null>(null);useEffect(()=>{const service=()=>location.pathname==="/service"||location.pathname.startsWith("/service/")||location.pathname.startsWith("/services/");const click=(e:MouseEvent)=>{if(!service())return;const t=e.target;if(!(t instanceof Element))return;const img=t.closest("main img") as HTMLImageElement|null;if(!img||!img.currentSrc||img.closest("header,nav,footer,button,.avatar,.logo,.icon"))return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();setSrc(img.currentSrc)};const esc=(e:KeyboardEvent)=>{if(e.key==="Escape")setSrc(null)};document.addEventListener("click",click,true);document.addEventListener("keydown",esc);return()=>{document.removeEventListener("click",click,true);document.removeEventListener("keydown",esc)}},[]);if(!src)return null;return <div className="saharaMediaLightbox" role="dialog" aria-modal="true" onClick={()=>setSrc(null)}><button className="saharaMediaLightboxClose" type="button" aria-label="Close" onClick={e=>{e.stopPropagation();setSrc(null)}}>×</button><img src={src} alt="Enlarged service image" onClick={e=>e.stopPropagation()}/></div>}
