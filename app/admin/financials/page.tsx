"use client";
import { useEffect,useState } from "react";
import { getClientAuth } from "@/lib/firebase-client";
export default function AdminFinancials(){const [data,setData]=useState<any>(null);const [q,setQ]=useState("");const [error,setError]=useState("");useEffect(()=>{(async()=>{try{const u=getClientAuth().currentUser;if(!u)throw new Error("Sign in required");const token=await u.getIdToken(true);const r=await fetch("/api/admin/financials",{headers:{authorization:`Bearer ${token}`}});const j=await r.json();if(!r.ok)throw new Error(j.error||"Unable to load financials");setData(j)}catch(e){setError(e instanceof Error?e.message:"Unable to load financials")}})()},[]);const sellers=(data?.sellers||[]).filter((s:any)=>`${s.name} ${s.email}`.toLowerCase().includes(q.toLowerCase()));return <main style={{maxWidth:1200,margin:"30px auto",padding:24,fontFamily:"Arial"}}><h1>Admin Financial Intelligence</h1><p>Private administrator-only financial data.</p>{error&&<p>{error}</p>}{data&&<><section style={{display:"grid",gridTemplateColumns:"repeat(5,1fr)",gap:12}}>{[["Marketplace revenue",data.grossCents],["Seller earnings",data.sellerEarningsCents],["Platform fees",data.platformFeesCents],["Pending withdrawals",data.pendingWithdrawalsCents],["Paid withdrawals",data.paidWithdrawalsCents]].map(([l,v])=><article key={String(l)} style={{padding:18,border:"1px solid #ddd",borderRadius:12}}><small>{l}</small><h2>€{(Number(v)/100).toFixed(2)}</h2></article>)}</section><p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search seller or email" style={{padding:12,width:"100%",margin:"24px 0"}}/></p><table style={{width:"100%",borderCollapse:"collapse"}}><thead><tr><th align="left">Seller</th><th align="left">Email</th><th align="right">Released</th><th align="right">Pending</th><th align="right">Available</th></tr></thead><tbody>{sellers.map((s:any)=><tr key={s.id}><td>{s.name||"—"}</td><td>{s.email}</td><td align="right">€{(s.totalEarningsCents/100).toFixed(2)}</td><td align="right">€{(s.pendingEarningsCents/100).toFixed(2)}</td><td align="right">€{(s.availableBalanceCents/100).toFixed(2)}</td></tr>)}</tbody></table></>}</main>}
