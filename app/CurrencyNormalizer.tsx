"use client";
import {useLayoutEffect} from "react";
const replaceCurrency=(root:ParentNode)=>{const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes:Text[]=[];while(walker.nextNode())nodes.push(walker.currentNode as Text);for(const node of nodes){const p=node.parentElement;if(!p||["SCRIPT","STYLE","TEXTAREA","INPUT","OPTION","SVG"].includes(p.tagName))continue;const a=node.nodeValue||"";const b=a.replace(/€/g,"₮").replace(/\bEUR\b/gi,"USDT").replace(/\bUSD\b/gi,"USDT").replace(/\bEURO\b/gi,"USDT").replace(/\bDOLLARS?\b/gi,"USDT").replace(/\$(?=\s*\d)/g,"₮").replace(/Live currency conversion/gi,"USDT pricing").replace(/Indicative automatic conversion/gi,"All prices are in USDT");if(a!==b)node.nodeValue=b;}};
const normalizeNow=()=>{try{localStorage.setItem("saharasnow_currency","USDT")}catch{}document.documentElement.dataset.currency="USDT";document.querySelectorAll<HTMLSelectElement>('select[aria-label="Currency"]').forEach(x=>x.remove());if(document.body)replaceCurrency(document.body);};
export default function CurrencyNormalizer(){useLayoutEffect(()=>{normalizeNow();const onRestore=()=>normalizeNow();window.addEventListener("pageshow",onRestore);window.addEventListener("popstate",onRestore);window.addEventListener("sahara-layout-restore",onRestore);return()=>{window.removeEventListener("pageshow",onRestore);window.removeEventListener("popstate",onRestore);window.removeEventListener("sahara-layout-restore",onRestore)}},[]);return null}
