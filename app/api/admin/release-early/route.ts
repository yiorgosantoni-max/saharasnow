import {NextRequest,NextResponse} from "next/server";
import {FieldValue} from "firebase-admin/firestore";
import {z} from "zod";
import {db} from "@/lib/firebase-admin";
import {publicError,userFromRequest} from "@/lib/server";
import {notifyUser} from "@/lib/notifications";
const bodySchema=z.object({orderId:z.string().min(1)});
export async function POST(req:NextRequest){try{const adminUser=await userFromRequest(req);const admin=(await db.collection("users").doc(adminUser.uid).get()).data()||{};if(admin.role!=="admin"&&String(admin.email||adminUser.email||"").toLowerCase()!=="yiorgosantoni@gmail.com")return NextResponse.json({error:"Administrator access required."},{status:403});const {orderId}=bodySchema.parse(await req.json());const orderRef=db.collection("orders").doc(orderId);let sellerId="",amount=0;await db.runTransaction(async tx=>{const snap=await tx.get(orderRef);if(!snap.exists)throw new Error("Order not found.");const order=snap.data()||{};sellerId=String(order.sellerId||"");amount=Math.max(0,Number(order.sellerEarnings??order.sellerAmount??order.totalCents?Number(order.totalCents)/100:0));if(!sellerId)throw new Error("Order seller not found.");const sellerRef=db.collection("users").doc(sellerId),sellerSnap=await tx.get(sellerRef);const seller=sellerSnap.data()||{};const current=Math.max(0,Number(seller.balance||seller.availableBalance||0));tx.set(sellerRef,{balance:current+amount,availableBalance:current+amount,updatedAt:FieldValue.serverTimestamp()},{merge:true});tx.set(orderRef,{status:"completed",clearanceStatus:"released",releasedEarly:true,releasedAt:FieldValue.serverTimestamp(),releasedBy:adminUser.uid,updatedAt:FieldValue.serverTimestamp()},{merge:true});});await notifyUser({userId:sellerId,type:"earnings_released_early",eventId:orderId,title:"Earnings released early",message:`An administrator released your pending earnings of ${amount.toFixed(2)} early. The funds are now available in your balance.`,link:"/?seller=1",email:true});return NextResponse.json({ok:true,orderId,amount});}catch(e){const x=e instanceof z.ZodError?{message:"Invalid release request.",status:400}:publicError(e);return NextResponse.json({error:x.message},{status:x.status});}}
