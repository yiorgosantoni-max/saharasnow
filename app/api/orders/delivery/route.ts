import {FieldValue}from"firebase-admin/firestore";
import {NextRequest,NextResponse}from"next/server";
import {z}from"zod";
import {db}from"@/lib/firebase-admin";
import {notifyAdminInApp,notifyUser}from"@/lib/notifications";
import {publicError,userFromRequest}from"@/lib/server";
export const runtime="nodejs";
const file=z.object({name:z.string().min(1).max(255),url:z.string().url().max(2000),size:z.number().int().positive().max(100*1024*1024),type:z.string().max(255).optional()});
const schema=z.object({orderId:z.string().min(1),message:z.string().trim().max(3000).default(""),attachments:z.array(file).min(1).max(10)});
export async function POST(req:NextRequest){try{const user=await userFromRequest(req),body=schema.parse(await req.json());const ref=db.collection("orders").doc(body.orderId);let order:any;await db.runTransaction(async tx=>{const snap=await tx.get(ref);if(!snap.exists)throw Error("Order not found.");order=snap.data()||{};if(String(order.sellerId)!==user.uid)throw Error("Only the seller can deliver this order.");if(!["paid","in-progress"].includes(String(order.status)))throw Error("This order is not ready for delivery.");const now=new Date(),ends=new Date(now.getTime()+5*24*60*60*1000);tx.set(ref,{status:"delivered",deliveryStatus:"delivered",delivery:{message:body.message,attachments:body.attachments,deliveredAt:FieldValue.serverTimestamp()},clearanceStartsAt:FieldValue.serverTimestamp(),clearanceEndsAt:ends,disputeWindowEndsAt:ends,updatedAt:FieldValue.serverTimestamp()},{merge:true});});await ref.collection("events").add({type:"delivery_submitted",message:body.message||`${body.attachments.length} delivery file(s) uploaded.`,attachments:body.attachments,actorId:user.uid,createdAt:FieldValue.serverTimestamp()});const number=String(order.orderNumber||body.orderId);await notifyUser({userId:String(order.buyerId),type:"order_delivered",eventId:`${body.orderId}_delivered`,title:`Order #${number} delivered`,message:`Your seller delivered ${body.attachments.length} file(s). Your five-day review and dispute period has started.`,link:`/?order=${body.orderId}`,email:true});await notifyAdminInApp({type:"order_delivery",eventId:body.orderId,title:`Order #${number} delivered`,message:`The five-day clearance timer has started.`,link:"/admin"});return NextResponse.json({ok:true,orderId:body.orderId,orderNumber:number,status:"delivered",clearanceEndsAt:new Date(Date.now()+5*24*60*60*1000).toISOString()});}catch(e){const x=e instanceof z.ZodError?{message:"Invalid delivery request.",status:400}:publicError(e);return NextResponse.json({error:x.message},{status:x.status});}}
