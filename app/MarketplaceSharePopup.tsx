"use client";

// Kept as a compatibility export for older imports. The shared implementation
// now lives in CardSharePopup so every marketplace surface uses the same modal,
// X branding, and real engagement counters.
export { default } from "./CardSharePopup";
