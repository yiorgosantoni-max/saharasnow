"use client";
import {useEffect} from "react";

export default function CustomOrderNotificationTabs(){useEffect(()=>{const timer=window.setTimeout(()=>{const notificationRoots=[...document.querySelectorAll<HTMLElement>("main,aside")].filter(el=>/notifications|notification/i.test(el.innerText||""));if(!notificationRoots.length)return;notificationRoots.forEach(root=>{if(root.querySelector(".saharaCustomOrderTabs"))return;const host=root.querySelector<HTMLElement>("nav,[role='tablist']")||root;const wrap=document.createElement("div");wrap.className="saharaCustomOrderTabs";wrap.innerHTML=`<button type="button" data-custom-role="seller">📥 CUSTOM ORDER REQUEST</button><button type="button" data-custom-role="buyer">💳 SEND CUSTOM ORDER</button>`;host.appendChild(wrap);const open=(role:string)=>{window.dispatchEvent(new CustomEvent("saharasnow:custom-order",{detail:{role}}));};wrap.querySelectorAll<HTMLButtonElement>("button").forEach(b=>b.onclick=()=>open(b.dataset.customRole||"buyer"));});},200);return()=>window.clearTimeout(timer)},[]);return <style>{`.saharaCustomOrderTabs{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0}.saharaCustomOrderTabs button{border:0;border-radius:10px;padding:10px 13px;font-weight:800;cursor:pointer;background:#f1f5f9}.saharaCustomOrderTabs button:hover{transform:translateY(-1px);box-shadow:0 5px 14px rgba(0,0,0,.1)}`}</style>}
